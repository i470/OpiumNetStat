<UserControl x:Class="OpiumNetStat.Views.TrafficView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpiumNetStat.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1200">
    <Grid>
       
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="300"  />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <materialDesign:Card Grid.Row="0"  
                                 materialDesign:ShadowAssist.ShadowDepth="Depth1" 
                                 Padding="3" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition  />
                    </Grid.ColumnDefinitions>

                   
                    <Border Grid.Column="0" Padding="6" MinHeight="100">
                        <Button
  Style="{StaticResource MaterialDesignFloatingActionButton}"
  materialDesign:ButtonProgressAssist.IsIndicatorVisible="True"
  materialDesign:ButtonProgressAssist.Value="0"
  materialDesign:ButtonProgressAssist.IsIndeterminate="True"
  Content="{materialDesign:PackIcon Kind=AccessPoint}" />
                    </Border>
                    <Border Grid.Column="1" Margin="5">
                        <TextBlock Text="Traffic inspection is done by capturing traffic via proxy which allows to intercept browser traffic and analyze connections in greater detail. Backlight inspection will analyze connection in a background for beacons, trackers and keyloggers (!) Proxy connection might slow down your web browsing or interfer with other connections such as VPN. Keep it turned off if you experience connections issues and not actively observing traffic " FontSize="15" Foreground="Gray" HorizontalAlignment="Stretch" VerticalAlignment="Center" TextWrapping="Wrap" />
                    </Border>
                </Grid>
              
            </materialDesign:Card>
            <ListView Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch" ItemsSource="{Binding Sessions}"
                  SelectedItem="{Binding SelectedSession}" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="App" DisplayMemberBinding="{Binding Process}" />
                        <GridViewColumn Header="Protocol" DisplayMemberBinding="{Binding Protocol}" />
                        <GridViewColumn Header="Host" DisplayMemberBinding="{Binding Host}" />
                        <GridViewColumn Header="Sent" DisplayMemberBinding="{Binding SentDataCount}" />
                        <GridViewColumn Header="Received" DisplayMemberBinding="{Binding ReceivedDataCount}" />
                        <GridViewColumn Header="IP" DisplayMemberBinding="{Binding ClientRemoteEndPoint}" />
                        <GridViewColumn Header="3rd Party Cookies" DisplayMemberBinding="{Binding ClientRemoteEndPoint}" />
                        <GridViewColumn Header="Trackers" DisplayMemberBinding="{Binding ClientRemoteEndPoint}" />
                        <GridViewColumn Header="Keyloggers" DisplayMemberBinding="{Binding ClientRemoteEndPoint}" />
                        <GridViewColumn Header="Facebook Pixel" DisplayMemberBinding="{Binding ClientRemoteEndPoint}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
        <Grid Grid.Column="2">
            <materialDesign:Card>
                
            </materialDesign:Card>
        </Grid>
    </Grid>
  
</UserControl>
